class User < ApplicationRecord

  # def self.from_omniauth(auth)
  #   User.find_or_create_by(uid: auth[:uid], provider: response[:provider]) do |u|
  #     u.username = auth[:info][:name]
  #     u.email = auth[:info][:email]
  #   end
  # end
end
